import React from "react";
// import { Link } from "react-router-dom";

export const NavBar = () => {
  return (
    <nav className="navbar">
      <h3>Profile</h3>
      <select
      name="selector"
      id=""
      onChange={(event) => {}}
      >

      </select>
    </nav>
  )
}